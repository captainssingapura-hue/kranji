// AUTO-GENERATED by Depth2GenerateMain - do not edit by hand.
package kranji.common.depth2;

import kranji.zi.ComposedZi;

import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

/**
 * Depth-2 composed characters: at least one direct component is itself a
 * depth-1 composition.
 *
 * <p>Records are partitioned by pinyin (initial, final, tone) — see the
 * {@code <initial>/<Final><Tone>.java} sub-package files. This class
 * concatenates every per-initial aggregator into a single immutable list.</p>
 */
public final class Depth2 {

    private Depth2() {}

    /** All depth-2 composed characters. */
    public static final List<ComposedZi> ALL;

    static {
        var list = new ArrayList<ComposedZi>();
        list.addAll(Depth2B.ALL);
        list.addAll(Depth2Ch.ALL);
        list.addAll(Depth2D.ALL);
        list.addAll(Depth2F.ALL);
        list.addAll(Depth2G.ALL);
        list.addAll(Depth2J.ALL);
        list.addAll(Depth2L.ALL);
        list.addAll(Depth2M.ALL);
        list.addAll(Depth2N.ALL);
        list.addAll(Depth2P.ALL);
        list.addAll(Depth2Q.ALL);
        list.addAll(Depth2S.ALL);
        list.addAll(Depth2Sh.ALL);
        list.addAll(Depth2T.ALL);
        list.addAll(Depth2Z.ALL);
        list.addAll(Depth2Zero.ALL);
        list.addAll(Depth2Zh.ALL);
        ALL = Collections.unmodifiableList(list);
    }
}
