/**
 * Article preparation tooling.
 *
 * <p>Build-time only. Parses the override-only source format and runs the
 * polyphony check against the committed baseline, so no ambiguous reading
 * reaches a reader without having been considered.</p>
 */
package kranji.reading.prep;
