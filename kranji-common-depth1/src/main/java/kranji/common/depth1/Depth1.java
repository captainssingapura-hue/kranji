// AUTO-GENERATED by Depth1GenerateMain - do not edit by hand.
package kranji.common.depth1;

import kranji.zi.ComposedZi;

import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

/**
 * Depth-1 composed characters: every leaf component is a singular block (depth 0).
 *
 * <p>Records are partitioned by pinyin (initial, final, tone) — see the
 * {@code <initial>/<Final><Tone>.java} sub-package files. This class
 * concatenates every per-initial aggregator into a single immutable list.</p>
 */
public final class Depth1 {

    private Depth1() {}

    /** All depth-1 composed characters. */
    public static final List<ComposedZi> ALL;

    static {
        var list = new ArrayList<ComposedZi>();
        list.addAll(Depth1B.ALL);
        list.addAll(Depth1C.ALL);
        list.addAll(Depth1Ch.ALL);
        list.addAll(Depth1D.ALL);
        list.addAll(Depth1F.ALL);
        list.addAll(Depth1G.ALL);
        list.addAll(Depth1H.ALL);
        list.addAll(Depth1J.ALL);
        list.addAll(Depth1K.ALL);
        list.addAll(Depth1L.ALL);
        list.addAll(Depth1M.ALL);
        list.addAll(Depth1N.ALL);
        list.addAll(Depth1P.ALL);
        list.addAll(Depth1Q.ALL);
        list.addAll(Depth1R.ALL);
        list.addAll(Depth1S.ALL);
        list.addAll(Depth1Sh.ALL);
        list.addAll(Depth1T.ALL);
        list.addAll(Depth1X.ALL);
        list.addAll(Depth1Z.ALL);
        list.addAll(Depth1Zero.ALL);
        list.addAll(Depth1Zh.ALL);
        ALL = Collections.unmodifiableList(list);
    }
}
